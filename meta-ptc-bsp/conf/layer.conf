# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-ptc-bsp"
BBFILE_PATTERN_meta-ptc-bsp = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-ptc-bsp = "6"

LAYERDEPENDS_meta-ptc-bsp = "core"
LAYERSERIES_COMPAT_meta-ptc-bsp = "scarthgap"
